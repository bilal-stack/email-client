# Mission (lite)

AI-first universal email PWA. Gmail + O365 + IMAP behind one `IEmailProvider` interface. Claude provides per-thread summaries (cached), priority scoring on every new message (tool-use schema), and tone-matched draft replies (streamed). Email only — no calendar / contacts / tasks. Spec-driven multi-agent build; judged on Claude Code discipline as much as the product.
